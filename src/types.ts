// type SeriesSize = 'sm' | 'md' | 'lg';

import { ExcalidrawOptions } from 'components/SimpleEditor';

export interface SimpleOptions {
  // text: string;
  // showSeriesCount: boolean;
  // seriesCountSize: SeriesSize;

  excalidrawOptions?: ExcalidrawOptions;
}
